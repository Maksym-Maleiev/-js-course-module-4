/*
 * Method 'every()'
 */

// Усі елементи більше чи рівні нулю? - так [1, 2, 3, 4, 5].every((value) => value >= 0); // true // Усі елементи більше чи рівні нулю? - ні [1, 2, 3, -10, 4, 5].every((value) => value >= 0); // false
